This project only contains engine part of flow tool.

Flow.dat – file based flow data.

Flow Model – what our flow diagram looks like.

Flow Node – mini uint in the Flow model

Flow Subflow – subflow of flow model

getEntryNode()
